let x = ((expirationCookie == undefined) ? '' : ('; expires=' + expirationCookie.toGMTString()))

// Condition ? (x = if condition is true) : (y = if condition is false)

/* if condition {
    return x
} else {
    return y
}

*/

